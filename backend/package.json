{
  "name": "workspace-backend",
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types",
    "migration:create": "wrangler d1 migrations create workspace-db",
    "migration:apply": "wrangler d1 migrations apply workspace-db --local"
  },
  "dependencies": {
    "hono": "^4.1.0",
    "jose": "^5.2.3",
    "google-auth-library": "^9.7.0"
  },
  "devDependencies": {
    "wrangler": "^3.35.0"
  }
}
